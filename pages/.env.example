# Copy to a private local `.env.local` or shell environment for Pages dev.
# The Google value is a public Web OAuth client id, not a client secret.

VITE_WORKER_API_BASE_URL=http://127.0.0.1:8787
VITE_GOOGLE_OIDC_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
