# DECISIONS

Durable product, architecture, release, deployment, and risk decisions.

Do not rewrite historical decisions. Add a new row when a decision changes.

Decision status values:

- `Active`
- `Superseded`
- `Dropped`
- `Pending PO`

| ID | Decision | Status | Notes |
|---|---|---|---|
| D-001 | Use lightweight three-role Codex governance for TimeWhere until Product Owner chooses a different mode. | Active | Based on `_imports/governance-template/README.md` and `MIGRATION_GUIDE.md`; template default is Lightweight. |
| D-002 | Product Owner keeps final product, scope, risk, and release/deployment authority. | Active | Imported from governance template role contracts and root rules. |
| D-003 | Publish, deploy, submit, tag, merge, or release actions require explicit Product Owner approval. | Active | Imported from `AGENTS_TEMPLATE.md`, `PROJECT_WORKFLOW_TEMPLATE.md`, and `ReleaseMg.md`. |
| D-004 | Exact prompt-listed source files were not all present; initialization used actual imported template files available in `_imports/governance-template/`. | Superseded | Superseded by D-006. Missing: `EXPORT_README.md`, `SOURCE_INVENTORY.md`, non-template root filenames. Present equivalents included `README.md`, `MIGRATION_GUIDE.md`, and `*_TEMPLATE.md`. |
| D-005 | Governance mode is Lightweight. | Active | Product Owner decision for MVP build bootstrap. |
| D-006 | `_imports/governance-template/README.md` and `MIGRATION_GUIDE.md` are accepted as partial equivalents for missing `EXPORT_README.md` and `SOURCE_INVENTORY.md`. | Active | Product Owner decision for governance import source resolution. |
| D-007 | Active release target is Internal MVP acceptance. | Active | Product Owner decision; not a public release or Chrome Web Store submission target. |
| D-008 | Version naming uses package / extension release version `0.1.0` and design-doc version `v2.3`. | Superseded | Superseded by D-022 for the internal testing release version. Product Owner decision; package/release identity and design document lineage are tracked separately. |
| D-009 | MVP scope is local-first MVP. | Active | Product Owner decision; Google Sync implementation and other advanced scheduling/release items are excluded from this MVP package. |
| D-010 | Product Owner approved Internal MVP acceptance. | Active | Approved on 2026-05-12 after releaseMg recheck reported `READY FOR PRODUCT OWNER DECISION`. This is not public release readiness and does not approve Chrome Web Store, tag, push, merge, publish, deploy, upload, or submit. |
| D-011 | Open Project Baseline Audit before further feature work. | Active | Product Owner approved a read-only code and documentation audit on 2026-05-12. No code changes are approved during the audit without explicit Product Owner approval. |
| D-012 | Approve Phase 1 corrective Build&Test package. | Active | Product Owner approved on 2026-05-12. Scope is limited to Popup task actions, Daily Settle task-pool consistency, and minimal regression coverage. This does not approve public release, Chrome Web Store, tag, push, merge, publish, deploy, upload, or submit. |
| D-013 | Canonical id strategy: `tasks`, `containers`, `events`, and `habits` use string UUID ids; planner helper records may remain numeric for now. | Active | Product Owner approved Product&Project Mg recommendation on 2026-05-12. No schema migration is approved by this decision alone. |
| D-014 | Product&Project Mg may select and initiate the next baseline work package. | Active | Product Owner instructed Product&Project Mg to decide the plan and execute on 2026-05-12. Product&Project Mg selected Phase 2A safety hardening before documentation baseline cleanup. This does not approve public release, Chrome Web Store, tag, push, merge, publish, deploy, upload, or submit. |
| D-015 | Buckets and labels are scoped to a Plan; labels stay lightly defined for now. Subject Plan default buckets are `上课`, `作业`, `单元测试`, `阶段考试`; `Other School Plan` default buckets are `事项`, `活动`, `申请`, `其他`; ManageBac is Task source metadata, not a bucket or label. | Active | Product Owner confirmed on 2026-05-14. Buckets are initialized from templates per Plan and may be edited inside each concrete Plan. No DB schema migration is approved by this decision alone. |
| D-016 | Manual task creation must require `due_date`; if `start_date` is empty, it defaults to the selected `due_date`. Task Board quick-add fields depend on current grouping: due-date grouping includes Bucket; bucket grouping includes start date and required due date; other groupings include start date, required due date, and Bucket. | Active | Product Owner confirmed on 2026-05-14. This is a manual creation and quick-add UX rule, not a DB schema migration. Imported/source tasks may still be governed by their own source-specific rules. |
| D-017 | Task Date Arrange is now in current baseline stabilization scope. Arrange may calculate `start_date` and priority upgrades, but it must preview changes first and write task updates only after user confirmation. Priority may be upgraded to `important` / `urgent` but must not be downgraded. Daily Settle Layer 2 remains an overflow receiver for all overflow tasks, not only urgent or overdue tasks. ManageBac source tasks allow local execution and scheduling fields (`progress`, `completed_at`, `start_date`, `priority`) while source content fields (`title`, `due_date` / `deadline`, description/source content, source UID/URL/metadata) remain read-only. | Superseded | Product Owner confirmed on 2026-05-14. Superseded by D-024 for Task Arrange trigger, throttle, and write path; local scheduling/source-field protection remains carried forward by D-024. This decision did not approve background alarms, Google Sync, notifications, Chrome Web Store work, public release, tag, push, merge, publish, deploy, upload, or submit. |
| D-018 | Six-hour management automation is unified under Dashboard entry. Popup, Calendar, and Planner page opening must not silently run six-hour Arrange or ManageBac sync checks. Dashboard / Focus entry checks every six hours, combines Task Date Arrange preview and ManageBac new-event preview, persists a `management_review_pending` state when user confirmation is required, and opens a full confirmation page. Planner `my ManageBac` manual sync is ManageBac-only and opens the same confirmation page. Users must either confirm selected items or skip all before the pending management review is complete. | Superseded | Product Owner confirmed on 2026-05-15. Superseded by D-024 for Task Arrange trigger, throttle, and write path; ManageBac new-event creation remains confirmation-based. This decision did not approve background alarms, automatic public release work, tag, push, merge, publish, deploy, upload, or submit. |
| D-019 | TimeWhere remains fully local-first and fully usable without a Google account. Google account configuration is optional and is used only for durable cloud storage and cross-device data synchronization. First Google sync design uses Google Drive `appDataFolder`, starts with manual bidirectional sync plus conflict confirmation rather than background automation, synchronizes the ManageBac ICS link because Product Owner requires it, labels the UI as `Google 数据同步` rather than login, and defers displaying Google email/account identity to a later implementation step. | Active | Product Owner confirmed on 2026-05-15. This decision approves product/design direction for the next Google data sync work package. It does not approve Chrome Web Store submission, public release, tag, merge, deploy, publish, upload, or unreviewed code changes. |
| D-020 | Google data sync v1 is automatic bidirectional sync, not backup-only. IndexedDB remains the runtime source of truth; Google Drive `appDataFolder` stores `timewhere-sync-v1.json` as the cross-device exchange document. Sync uses record-level merge, dirty metadata, tombstones for deletes, and non-blocking conflict confirmation. Opening Settings / Dashboard / Planner / Calendar / Popup may trigger throttled sync when connected, and local writes may debounce a sync attempt. Conflicts must not be silently overwritten; dangerous actions “upload this device over cloud” and “restore cloud over this device” require explicit confirmation. | Active | Product Owner approved implementation on 2026-05-15. This does not approve Google Calendar / Google Tasks integration, background alarms, public release, Chrome Web Store submission, tag, merge, deploy, publish, or release. |
| D-021 | System task reminder notifications are approved for current baseline stabilization. Chrome `notifications` and `alarms` permissions may be used only for local task reminders. Reminders cover explicit `schedule_time` tasks and Daily Settle current-container tasks without `schedule_time`; explicit `schedule_time` wins when both apply. Fixed rules: one-minute advance reminder, then every 15 minutes during the task/window while unfinished. Settings must provide an on/off control and a manual test notification action. | Active | Product Owner approved implementation on 2026-05-16. This does not approve background alarm based Arrange, background ManageBac sync, Google Calendar / Google Tasks integration, Chrome Web Store submission, public release, tag, merge, deploy, publish, upload, or submit. |
| D-022 | Generate internal testing release version `0.1.1` as a local artifact for validation. This permits package / manifest version bump and local `dist` artifact generation only. | Active | Product Owner requested an internal release build on 2026-05-17. This does not approve public release, Chrome Web Store submission, git tag, merge, deploy, publish, or external release upload. |
| D-023 | Prepare Chrome Web Store Private testing submission materials for TimeWhere `0.2.0`, with Chinese-first listing copy. | Active | Product Owner approved material preparation on 2026-05-18. Scope includes local package generation, releaseMg readiness evidence, CWS listing/privacy/test-instruction materials, and clean store assets. This does not approve actual Chrome Web Store upload, Submit for Review, publish, public listing, git tag, merge, push, deploy, or release. |
| D-024 | Task Date Arrange page-open automation is approved for Dashboard / Focus, Planner / Task Board, and Calendar. It is no longer six-hour throttled: each page open may run the shared Arrange helper and directly apply eligible local scheduling changes. Arrange may update only local scheduling fields (`start_date` and priority upgrades), must not downgrade priority, must not edit source content or completed tasks, and must not use background alarms. Same-subject timetable matching may use same-day dates (`>=` base date); arbitrary timetable fallback remains future-only. Plan and Calendar diagnostics may expose safe Arrange preview snapshots with task title/source/date/priority fields. ManageBac new-event creation remains confirmation-based; Popup still does not run automatic Arrange on open. | Active | Product Owner confirmed on 2026-05-20. Supersedes D-017/D-018 for Task Arrange trigger, throttle, and write path only. Does not approve Chrome Web Store upload, Submit for Review, publish, public listing, tag, merge, push, deploy, release, Google Calendar / Google Tasks integration, background alarm based Arrange, or background ManageBac sync. |
| D-025 | Approve CWS `0.2.1` Purple Potassium policy-fix resubmission. | Active | Product Owner approved removing the unnecessary `tabs` permission, regenerating a sanitized `0.2.1` CWS package with lightweight verification, canceling the current CWS review, uploading the fixed package, and submitting it for review again. This does not approve automatic publish after review, public listing expansion, tag, merge, push, deploy, or release. |
| D-026 | Chrome toolbar action opens TimeWhere Side Panel as the primary quick surface. | Active | Current baseline source uses Chrome `sidePanel` permission and `side_panel.default_path=popup/sidepanel.html`; `popup.html` remains as reusable compact UI assets/runtime shell. Side Panel exposes current tasks, Dashboard/Task/Calendar/Settings navigation, in-place temporary task creation, and Daily Journal review. Dashboard current-task column also includes the same temporary-task quick-add pattern. This is a source/documentation sync decision only and does not approve a new CWS package upload, Submit for Review, automatic publish, public listing, tag, merge, deploy, or release. |
| D-027 | Approve CWS `0.2.2` Side Panel update submission and automatic publish after review. | Active | Product Owner approved publishing the accepted `0.2.1` Private testing package first, adding the specified trusted tester accounts in the CWS publisher settings, bumping the current Side Panel package to `0.2.2` because CWS rejects same-version uploads, uploading the sanitized `0.2.2` package, submitting it for review, and enabling automatic publish after review. No tag, merge, public listing expansion, or non-CWS deployment is approved. |
| D-028 | Approve CWS OAuth client correction package preparation for `0.2.3`. | Active | Product Owner authorized creating/configuring the Google Cloud Chrome Extension OAuth client for CWS extension ID `bokjekfjghliieopghopibmhjokgkjkb`, then instructed Build&Test to continue. Scope is local source/package preparation only: keep the development OAuth client in source for fixed unpacked extension ID `ogdjmelmfkfahppahhkkggdejjainbnd`, inject the CWS OAuth client into sanitized CWS packages, bump package/source version to `0.2.3`, and generate local package evidence. This does not approve CWS upload, Submit for Review, publish, public listing expansion, tag, merge, push, deploy, or release. |
| D-029 | Approve CWS `0.2.3` OAuth correction submission and automatic publish after review. | Active | Product Owner approved canceling the pending `0.2.2` CWS review if needed, uploading `dist/TimeWhere-0.2.3-private-cws-sanitized-20260524-041047.zip`, submitting it for review, and enabling automatic publish after review. releaseMg canceled the pending draft review, uploaded `0.2.3`, confirmed draft version `0.2.3` with expected permissions and no `tabs`, submitted it for review, and verified CWS status `待审核` on 2026-05-24. Public listing expansion, tag, merge, push, deploy, and non-CWS release remain unapproved. |
| D-030 | Adopt staged dual-platform architecture preparation for TimeWhere. | Superseded | Superseded by D-031 for desktop target and scope. Product Owner approved preparing a Shared Core + Chrome Extension Shell + Mac Electron Shell evolution path. Chrome Extension remains the current production shell; Electron is the Mac preview direction, not Swift/Tauri. IndexedDB / Dexie and Google Drive `appDataFolder` sync remain the MVP storage/sync model; do not migrate to SQLite, duplicate UI, or silently bind Google accounts / restore cloud data. This approves documentation, platform-boundary adapter work, reinstall recovery UX, and an Electron preview skeleton only. It does not approve Mac release, signing, auto-update, CWS submission, public listing expansion, tag, merge, push, deploy, or release. |
| D-031 | Build a standalone Windows desktop app with optional Chrome extension connection. | Active | Product Owner approved upgrading the Electron path from Mac preview to a desktop Electron package targeting Windows portable exe first. Windows must be independently usable without installing or connecting the Chrome extension. Chrome extension connection is optional and limited to a localhost nonce bridge that exchanges extension ID/version only, not TimeWhere task data. Desktop Google Sync uses Google Drive `appDataFolder` through an installed-app OAuth client ID with PKCE and no client secret; refresh tokens must be encrypted with Electron `safeStorage` or not saved. This approves local desktop implementation and Windows package generation only. It does not approve CWS submission of bridge permissions, public listing expansion, tag, merge, push, deploy, signing, installer, auto-update, or release. |
| D-032 | Use bundled Google Desktop OAuth client metadata secret for desktop sync. | Active | Product Owner approved replacing the D-031 Desktop OAuth `no client secret` clause with `PKCE + bundled Desktop client metadata secret` for the current internal desktop stage after Google token exchange required `client_secret` for the Desktop app client. The value must not be committed to git; internal desktop packaging generates a gitignored `desktop-oauth-secrets.js` module from `TIMEWHERE_GOOGLE_DESKTOP_CLIENT_SECRET` or local ignored config, then bundles it into the artifact. Ordinary users must not configure desktop OAuth secrets. Refresh tokens still require Electron `safeStorage`; a future true no-secret model would require a backend OAuth broker and is not in current scope. This does not approve release, signing, notarization, installer, CWS submission, tag movement, merge, deploy, or public publishing. |
| D-033 | Desktop task reminders use notification-click acknowledgement. | Active | Product Owner approved changing Electron desktop reminders so an unclicked task notification may repeat every minute within the current 15-minute reminder bucket. Clicking the system notification opens TimeWhere and acknowledges that bucket, stopping further repeats until the next 15-minute bucket if the task remains unfinished. Tray restore, menu opening, ordinary app focus, and closing a notification do not acknowledge the reminder. Chrome Extension reminder behavior remains governed by D-021. This does not approve macOS WidgetKit, internal reminder panel work, CWS changes, release, tag, merge, push, deploy, signing, notarization, installer, or public publishing. |
| D-034 | Add display-only macOS WidgetKit source preparation. | Active | Product Owner approved a macOS native WidgetKit companion that only displays current tasks plus today's completed and pending task counts. The widget reads a sanitized `timewhere-widget-v1.json` snapshot and must not read IndexedDB, Google sync state, OAuth tokens, client secrets, account identifiers, or raw local data. This is source and unsigned compile preparation only; it does not embed the widget into the Electron macOS zip, approve signing, notarization, App Group production setup, release, tag, merge, push, deploy, installer, or public publishing. Chrome Side Panel remains unchanged. |
| D-035 | Desktop Google Sync uses account-bound local data spaces. | Active | Product Owner approved binding Desktop Google Sync to the currently authorized Google account identity. Desktop OAuth requests OIDC account identity in addition to Drive `appDataFolder`; the local account key is derived from the Google subject and used only for local data-space/profile isolation and account display. Different Google accounts must not share the same Electron IndexedDB profile unless the first unowned local profile is explicitly bound. Account display keys remain excluded from cloud sync documents. This does not approve Chrome Extension account partitioning, automatic deletion of mixed legacy data, release, signing, notarization, installer, tag, merge, push, deploy, or public publishing. |
| D-036 | ManageBac source tasks keep source facts read-only while local execution fields are editable. | Active | Product Owner approved expanding ManageBac task local editability beyond progress/start/priority. Users may edit local execution/workflow fields (`progress`, `status`, `completed_at`, `start_date`, `priority`, `notes`/`description`, `schedule_time`, `duration`, `checklist`, `labels`, `bucket_id`), and ManageBac re-sync must preserve those local values. Source facts and identifiers (`title`, `due_date`/`deadline`, `plan_id`, subject fields, source metadata, `managebac_subject`, `readonly`) remain source-controlled and user read-only; user delete and recurrence operations remain blocked. This does not change ManageBac title/deadline sync, Google Sync, desktop packaging, release, signing, tag, merge, push, deploy, or public publishing. |
| D-037 | Desktop Google Sync runs as a renderer-resident background sync service while the Electron app is open. | Active | Product Owner approved strengthening Desktop sync from page-open/debounce-only triggers to a desktop runtime service. Because TimeWhere data remains in renderer IndexedDB, the service runs in the Electron renderer, serializes sync jobs, retries retryable network/Drive failures with backoff, pauses on conflicts, and resumes after conflicts are resolved. Chrome Extension sync behavior remains page-triggered under D-020. Conflicts must preserve detail records as the source of truth and must not be silently overwritten. This does not approve a backend service, schema migration, signing, notarization, installer, tag, merge, push, deploy, release, or public publishing. |
| D-038 | Task Arrange automatic scheduling fields are local derived sync metadata, not user edits. | Active | Product Owner approved treating Task Arrange automatic writes to task `start_date` and priority as local derived scheduling state for Google Sync conflict purposes. Arrange-derived values must not update user `updated_at`, must not mark the task dirty by themselves, and must compare against the last synced base values so identical cloud/user records do not create conflict noise. If the user manually edits those fields, the derived marker is cleared and the edit becomes a normal sync change. This does not change Task Arrange product rules, manual task edits, Google Drive storage format beyond sync metadata, signing, packaging, tag, merge, push, deploy, release, or public publishing. |
| D-039 | Task reminders aggregate current work into one notification. | Superseded | Product Owner approved changing reminder semantics from per-task notifications to a single aggregated "current work is waiting" notification. When reminders are due, TimeWhere shows a short current-work list with up to three task titles and a hidden-count summary instead of a single task description; notes/description must not appear in the system notification. Desktop may repeat an unacknowledged aggregated reminder every 3 minutes within the current 15-minute bucket; clicking the notification or actively opening/restoring TimeWhere acknowledges that bucket. Superseded by D-041 for Desktop reminder lifecycle and acknowledgement rules. Chrome Extension keeps the existing 15-minute bucket dedupe. This does not change task data, Google Sync, Widget, package generation, CWS submission, signing, tag, merge, push, deploy, release, or public publishing. |
| D-040 | Desktop Google Sync separates local disconnect from Google OAuth revocation. | Active | Product Owner approved treating "disconnect Google sync" as a local device action that clears/stops this device's saved sync authorization without calling Google's revoke endpoint, so it should not invalidate other TimeWhere devices using the same OAuth project. A separate explicit dangerous action may revoke Google authorization and must warn that other devices can require reauthorization. Desktop OAuth must keep sanitized client metadata diagnostics so refresh-token failures can be attributed to revoke, OAuth metadata mismatch, token churn, Google testing state, or Workspace session policy without recording tokens, emails, cookies, OAuth secrets, or local private paths. This does not approve release, signing, packaging, tag, merge, push, deploy, or public publishing. |
| D-041 | Desktop task reminders use a current-work reminder session state machine. | Active | Product Owner approved refactoring Desktop reminders from bucket acknowledgement into a local work-reminder session. The system notification represents "current work is waiting" rather than a specific task; clicking it opens TimeWhere without a task deep link, handles the current notification, and schedules a 30-minute execution check. Closing the system notification does not complete or handle the reminder; if work remains pending, Desktop may notify again after a 1-minute cooldown. A visible notification should not be repeated. A user may stop the current reminder session in-app. Chrome Extension adoption of this model is recorded separately in D-042. This does not change task data, Google Sync, Widget, package generation, CWS submission, signing, tag, merge, push, deploy, release, or public publishing. |
| D-042 | Chrome Extension task reminders adopt the current-work reminder session model. | Active | Product Owner approved syncing the D-041 reminder lifecycle back to the Chrome Extension. Chrome task reminders now represent aggregated current work rather than a specific task, use `chrome.storage.local` to persist a local work-reminder session, open TimeWhere without a task deep link on notification click, wait 30 minutes before execution check, and re-notify after a 1-minute cooldown when the user explicitly closes the notification while work remains pending. Chrome still uses the existing 1-minute alarm tick to evaluate state, and reminder state remains local-only. This does not change task data, Google Sync, Widget, package generation, CWS submission, signing, tag, merge, push, deploy, release, or public publishing. |
| D-043 | macOS self-signed internal release lane is approved for TimeWhere.app on limited managed Macs only. | Active | Product Owner approved planning and operator scripts for a self-signed internal macOS lane covering TimeWhere.app only. This lane uses an internal Code Signing certificate trusted manually on target Macs, and can produce signed/verified/checksummed internal artifacts for administrator-managed machines. This is not Developer ID signing, notarization, stapling, GitHub Release, public publishing, auto-update implementation, Mac App Store, or public distribution approval. |
| D-044 | Usage Agent is external to the TimeWhere project and future integration must follow an interface document. | Active | Product Owner clarified that the Usage Agent is not part of this repository, will not be developed or installed from the TimeWhere project, and TimeWhere must not sense or manage the Agent process, LaunchAgent, configuration, signing, permissions, retention, or storage implementation. Future TimeWhere work may consume usage data only through an explicit external interface document. Current Product Owner preferences for that external Agent are: no SQLite upgrade for v1, JSONL retained for 90 days, and per-user independent records. Developer ID / notarization remains out of current scope. TimeWhere must not claim user-directory usage logs are immutable or non-deletable. |
| D-045 | Chrome and Desktop Google Sync runtime semantics should be maximally aligned while preserving platform auth boundaries. | Active | Product Owner approved aligning Chrome Extension / Side Panel and Desktop Electron sync behavior around one shared page-hosted runtime state machine for serialized jobs, pending trigger coalescing, retry backoff, long-running status, and conflict pause. Both shells continue using the same Drive appDataFolder sync document and merge engine. Chrome remains a page-runtime implementation while a TimeWhere page or Side Panel is open, uses chrome.identity, and does not save refresh tokens or implement Desktop profile partitioning. Desktop remains an Electron runtime implementation with installed-app OAuth, bundled Desktop client metadata secret, safeStorage refresh-token protection, and account-bound local profiles. This does not approve CWS submission, public release, tag, push, merge, deploy, installer, auto-update, backend service, schema migration, Google Calendar/Tasks integration, or MV3 service-worker background sync. |
| D-046 | Provide a one-click DMG installer for the approved macOS self-signed internal lane. | Active | Product Owner approved an internal DMG for a small number of administrator-managed Macs. After the administrator Control-opens the installer and authorizes once, it may verify the pinned public certificate and signed Universal app, add that certificate to the System Keychain with Code Signing trust, atomically replace `/Applications/TimeWhere.app` with rollback, remove quarantine only from the verified installed TimeWhere.app, and launch it. The DMG must not contain a private key, `.p12`, password, token, or GitHub Secret. Manual installation remains the recovery path. This does not approve Developer ID signing, notarization, public distribution, GitHub Release, auto-update, MDM deployment, Mac App Store, or Gatekeeper disablement. |

## Change Rule

- New durable decisions must be appended.
- If a decision is replaced, mark the old one `Superseded` and add a new decision.
- Product Owner owns final product, scope, risk, and release/deployment decisions.
